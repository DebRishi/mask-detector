services:
  webapp:
    build: ./webapp
    restart: always
    ports:
      - 5000:3000
    depends_on:
      - server
  server:
    build: ./server
    restart: always
    ports:
      - 5050:5000